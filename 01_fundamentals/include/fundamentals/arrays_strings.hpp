#pragma once
// ╔══════════════════════════════════════════════════════════════════════════════╗
// ║  fundamentals/arrays_strings.hpp — C风格数组、C字符串、std::array、指针算术  ║
// ║  声明: 数组/字符串相关的常量和工具类型                                        ║
// ╚══════════════════════════════════════════════════════════════════════════════╝

#include <cstddef>      // std::size_t
#include <array>        // std::array

namespace fundamentals {

// ═══════════════════════════════════════════════════════════════════════════════
// 数组大小计算宏 (仅用于 C 风格数组)
// ═══════════════════════════════════════════════════════════════════════════════
// 注意: 此技巧只在数组没有退化为指针的作用域内有效！
// 一旦数组传给函数变成指针，sizeof 只能得到指针大小。
// 现代 C++ 推荐用 std::size() (C++17) 或 std::array::size()
template<typename T, std::size_t N>
constexpr std::size_t array_size(const T (&)[N]) noexcept {
    return N;
}

// ═══════════════════════════════════════════════════════════════════════════════
// 预定义的演示用数组
// ═══════════════════════════════════════════════════════════════════════════════
inline constexpr int demo_scores[] = {95, 82, 78, 91, 88};
inline constexpr std::size_t kDemoScoresSize = array_size(demo_scores);

inline constexpr int demo_matrix[2][3] = {
    {1, 2, 3},
    {4, 5, 6}
};

// ═══════════════════════════════════════════════════════════════════════════════
// 关键概念速查
// ═══════════════════════════════════════════════════════════════════════════════
// C 风格数组: 栈上分配的连续内存块，大小编译期确定
// C 字符串:   以 '\0' 结尾的 char 数组，不是一个类型，只是一个约定
// std::array: 对 C 风格数组的零开销包装 (C++11)，提供 STL 容器接口
// 数组退化:   当数组作为函数参数传递时退化为指针，丢失大小信息
// 指针算术:   ptr + n 不是加 n 个字节，而是加 n 个元素
//             ptr + 1 → 地址增加 sizeof(*ptr) 个字节

} // namespace fundamentals
