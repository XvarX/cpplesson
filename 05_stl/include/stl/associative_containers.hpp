// ╔══════════════════════════════════════════════════════════════════════════════╗
// ║  stl/associative_containers.hpp — 关联容器                                  ║
// ║  主题: set / map / multiset / multimap                                      ║
// ║  声明教学演示函数，供示例程序调用                                              ║
// ╚══════════════════════════════════════════════════════════════════════════════╝
#pragma once

#include "shared/lesson_utils.hpp"
#include <map>
#include <print>
#include <set>
#include <string>
#include <vector>

namespace stl_learn {

// ── Part 1: std::set — 有序唯一元素集合 ──────────────────────────────────────
// 是什么: 基于红黑树的有序集合，元素唯一且自动排序
// 为什么: 需要"自动排序 + 唯一性 + O(log n)查找"三者合一
// 什么时候用: 需要有序遍历；需要快速判断元素存在性；去重且需要排序
void part1_set();

// ── Part 2: std::map — 有序键值对 ─────────────────────────────────────────────
// 是什么: 键值对的有序映射，基于红黑树，按键排序，键唯一
// 为什么: 需要按键排序 + 关联值 + O(log n) 查找
// 什么时候用: 字典/配置/缓存/任何需要按键查找且需要有序遍历的场景
void part2_map();

// ── Part 3: std::multiset / std::multimap — 允许重复键 ────────────────────────
// 是什么: 与 set/map 相同，但允许重复的键
// 为什么: 需要存储重复键时（例如成绩列表中有相同分数、事件时间线中有同时发生的事件）
// 什么时候用: 需要有序 + 可重复的场景
void part3_multi();

// ── Part 4: 自定义比较器 ──────────────────────────────────────────────────────
// 是什么: 通过模板参数指定排序规则（默认为 std::less）
// 为什么: 需要自定义排序顺序（降序、按结构体字段、忽略大小写等）
// 什么时候用: 默认排序不满足需求时
void part4_comparator();

// ── Part 5: lower_bound / upper_bound / equal_range ───────────────────────────
// 是什么: 二分查找定位 — lower_bound 返回第一个 >= target 的位置;
//         upper_bound 返回第一个 > target 的位置; equal_range 返回两者的 pair
// 为什么: O(log n) 定位，是实现区间查询、分页、范围删除的基础
// 什么时候用: 查找"不小于某值的第一个元素"、"区间 [lo, hi) 中的所有元素"
void part5_bound();

// ── 常见陷阱 ─────────────────────────────────────────────────────────────────
void associative_containers_pitfalls();

// ── 练习 ────────────────────────────────────────────────────────────────────
void associative_containers_exercises();

} // namespace stl_learn
