#pragma once
// ╔══════════════════════════════════════════════════════════════════════════════╗
// ║  模块: C++20/23 新特性                                                       ║
// ║  主题: std::bit_cast、std::byteswap、位运算工具函数                          ║
// ║  目标: 掌握安全的位级类型转换、字节翻转、高效的位运算工具                    ║
// ║                                                                            ║
// ║  std::bit_cast<T>(src): 安全地将一个类型的位模式重解释为另一个类型           ║
// ║  WHY:  reinterpret_cast 违反严格别名规则 → UB                               ║
// ║        memcpy 可行但啰嗦 → bit_cast 一行搞定，constexpr 安全                 ║
// ║  要求: 源类型和目标类型大小必须相同，且都是 trivially copyable              ║
// ╚══════════════════════════════════════════════════════════════════════════════╝

#include <cstdint>

// ============================================================================
// Part 1: std::bit_cast — 安全的位级重新解释
// ============================================================================

// 经典场景1: 查看浮点数的 IEEE 754 内部表示
void inspect_float_bits(float value);

// 经典场景2: 快速求浮点数的倒数平方根 (Quake III 名算法的现代写法)
float fast_rsqrt(float number);

// 经典场景3: 网络字节序与主机字节序转换
enum class Endian { Little, Big };

Endian host_endian();

// ============================================================================
// Part 2: std::byteswap — 字节翻转 (C++23)
// ============================================================================
// 反转整数中字节的顺序 — 网络编程、数据格式转换的常用操作。
// WHY: 手动写移位代码易错，byteswap 一行搞定，编译器可优化为单条 CPU 指令 (如 bswap)。

// ============================================================================
// Part 3: 位运算工具函数 (C++20 <bit>)
// ============================================================================
// std::popcount(x)       — 统计二进制中 1 的个数 (汉明权重)
// std::countl_zero(x)    — 从高位开始连续 0 的个数
// std::countr_zero(x)    — 从低位开始连续 0 的个数
// std::has_single_bit(x) — 是否为 2 的幂 (只有一个 1 位)
// std::bit_ceil(x)       — 不小于 x 的最小 2 的幂
// std::bit_floor(x)      — 不大于 x 的最大 2 的幂
// std::bit_width(x)      — 表示 x 需要的二进制位数 (最高位的位置 + 1)
